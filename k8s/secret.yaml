apiVersion: v1
kind: Secret
metadata:
  name: air-quality-secrets
  namespace: air-quality
  labels:
    app: air-quality
type: Opaque
stringData:
  # Placeholder pour futures clés API
  # Note: Open-Meteo est actuellement gratuit et ne nécessite pas de clé
  # Mais ce secret sera utile pour d'autres services
  API_KEY_PLACEHOLDER: "not-used-yet"
  
  # Database credentials (pour migration future vers PostgreSQL)
  # DB_USERNAME: "airquality"
  # DB_PASSWORD: "changeme"
  
  # Pour encoder en base64 manuellement :
  # echo -n "valeur" | base64
